const proxy = require('http-proxy-middleware')

module.exports = function(app) {
  //app.use(proxy('auth/google', { target: 'http://localhost:8070/' })),
  //app.use(proxy('fairs', { target: 'http://localhost:9191' }))

}